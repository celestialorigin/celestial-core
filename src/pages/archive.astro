---
import Layout from "../layouts/Layout.astro";

const BASE = (import.meta.env.BASE_URL || "/").replace(/\/?$/, "/");
---

<Layout title="ARCHIVE" bodyClass="page-archive">
  <main class="archive-container fade-in-section">
    <div class="header-section">
      <h1 class="glitch-text" data-text="OBSERVATION ARCHIVE">OBSERVATION ARCHIVE</h1>

    </div>

    <!-- Main Card -->
    <div class="construction-card">
      <h2 class="card-title">アーカイブ状態：未定義</h2>
      <p class="construction-text">
        データは増えている。<br />
        整理されるかは、観測者次第。<br /><br />
        ここが完成する確率は、<br />
        今日あなたがここを見た確率と同じくらい。
      </p>

      <div class="nav-links">
        <a href={BASE} class="nav-btn">HOME</a>
        <a href={`${BASE}dialogues/`} class="nav-btn">DIALOGUES</a>
        <a href={`${BASE}creations/`} class="nav-btn">CREATIONS</a>
        <a href={`${BASE}contact/`} class="nav-btn">CONTACT</a>
      </div>
    </div>

    <!-- Coming Soon -->
    <div class="coming-soon">
      <h3 class="coming-title">将来やるかもしれないこと</h3>
      <ul class="coming-list">
        <li>分類</li>
        <li>整理</li>
        <li>統合</li>
        <li>何もしない</li>
      </ul>
    </div>

    <div class="back-home-link">
      <a href={BASE}>← HOMEへ戻る</a>
    </div>
  </main>
</Layout>

<style>
  .archive-container {
    max-width: 800px;
    margin: 80px auto 100px;
    padding: 0 20px;
    text-align: center;
  }

  .header-section {
    margin-bottom: 50px;
  }

  h1 {
    font-family: "Zen Kaku Gothic New", sans-serif;
    font-size: clamp(2rem, 5vw, 3rem);
    color: #e0faff;
    letter-spacing: 0.1em;
    margin-bottom: 1rem;
    text-shadow: 0 0 20px rgba(0, 255, 255, 0.3);
  }

  .subtitle {
    font-family: monospace;
    color: rgba(255, 255, 255, 0.7);
    font-size: 1rem;
    margin-bottom: 2rem;
  }

  /* Construction Card */
  .construction-card {
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 16px;
    padding: 40px 30px;
    margin-bottom: 50px;
    backdrop-filter: blur(8px);
    box-shadow: 0 0 30px rgba(0, 0, 0, 0.3);
  }

  .card-title {
    font-size: 1.5rem;
    color: var(--c-accent, #0ff);
    margin-bottom: 16px;
    letter-spacing: 0.05em;
  }

  .construction-text {
    font-size: 1rem;
    line-height: 1.8;
    color: rgba(255, 255, 255, 0.8);
    margin-bottom: 32px;
  }

  .nav-links {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 16px;
  }

  .nav-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 12px 24px;
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 999px;
    text-decoration: none;
    color: #fff;
    font-family: monospace;
    font-size: 1rem;
    background: rgba(255, 255, 255, 0.05);
    transition: all 0.2s ease;
    min-width: 140px;
  }

  .nav-btn:hover {
    background: rgba(0, 255, 255, 0.1);
    border-color: var(--c-accent, #0ff);
    box-shadow: 0 0 15px rgba(0, 255, 255, 0.2);
    transform: translateY(-2px);
  }

  /* Coming Soon */
  .coming-soon {
    max-width: 500px;
    margin: 0 auto;
    text-align: left;
    padding: 24px;
    border: 1px dashed rgba(255, 255, 255, 0.15);
    border-radius: 12px;
    background: rgba(0, 0, 0, 0.2);
  }

  .coming-title {
    font-size: 0.9rem;
    color: rgba(255, 255, 255, 0.5);
    margin-bottom: 12px;
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }

  .coming-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .coming-list li {
    font-size: 0.95rem;
    color: rgba(255, 255, 255, 0.7);
    padding: 6px 0;
    padding-left: 20px;
    position: relative;
  }

  .coming-list li::before {
    content: "•";
    color: var(--c-accent, #0ff);
    position: absolute;
    left: 0;
    top: 6px;
  }

  /* Back Home Link (Matched to Fragments/Real-time) */
  .back-home-link {
    margin-top: 4rem;
    text-align: center;
  }

  .back-home-link a {
    opacity: 0.8;
    text-decoration: none;
    font-size: 1.1rem;
    font-weight: 700;
    color: #fff;
    transition: all 0.3s ease;
    padding: 0.5rem 1rem;
    border-radius: 4px;
  }
  .back-home-link a:hover {
    opacity: 1;
    color: var(--c-accent);
    background: rgba(255, 255, 255, 0.05);
    box-shadow: 0 0 15px rgba(0, 255, 255, 0.2);
  }
</style>