---
import { logs } from '../../data/logs';

export function getStaticPaths() {
  return logs.map(log => ({
    params: { id: log.id },
  }));
}

const { id } = Astro.params;
return Astro.redirect(`/log/${id}`, 301);
---
